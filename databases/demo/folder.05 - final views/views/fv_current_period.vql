CREATE OR REPLACE VIEW fv_current_period FOLDER = '/05 - final views'
    PRIMARY KEY ( 'period' ) AS SELECT max(period) AS period FROM dv_immatriculations;

ALTER VIEW fv_current_period
 LAYOUT (dv_immatriculations = [20, 20, 235, 606]);

ALTER VIEW fv_current_period
 CACHE PARTIAL
     BATCHSIZEINCACHE DEFAULT TIMETOLIVEINCACHE DEFAULT;

