CREATE OR REPLACE REST WEBSERVICE cars
    CONNECTION (
        CHUNKSIZE = 1000
        CHUNKTIMEOUT = 90000
        QUERYTIMEOUT = 900000
        POOLENABLED = true
        POOLINITSIZE = 0
        POOLMAXACTIVE = 30
    )
    DEFAULTREPRESENTATION = HTML
    SUPPORTEDREPRESENTATIONS (HTML, XML, JSON)
    RESOURCES (
        VIEW brand FIELDS (
            brand_code : 'int', 
            brand_name : 'text'
        ),
        VIEW fact_car_details FIELDS (
            model_id : 'text', 
            mvid : 'int', 
            libcrb : 'text', 
            nbrcyl : 'int', 
            pkw : 'int', 
            cyd : 'int', 
            infco2 : 'int', 
            l100km : 'decimal', 
            taxrate : 'double'
        ),
        VIEW model FIELDS (
            brand_code : 'int', 
            model : 'text', 
            model_id : 'text'
        ),
        VIEW fact_car_statistics FIELDS (
            model_id : 'text', 
            bodywork_code : 'text', 
            count : 'long'
        ),
        VIEW bodywork FIELDS (
            bodywork_code : 'text', 
            bodywork_name : 'text', 
            bodywork_category : 'text', 
            bodywork_type : 'text'
        )
    )
    ASSOCIATIONS ( brand_model, model_car_details, model_cars_statistics, bodywork_cars_statistics )
    OPTIONS ( NULLVALUESASEMPTYXMLELEMENTS = false ) 
    OPENAPI2 ( API_VERSION = '1.0.0' ) 
    FOLDER = '/09 - data services';

