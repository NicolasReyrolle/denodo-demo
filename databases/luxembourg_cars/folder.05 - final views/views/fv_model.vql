CREATE OR REPLACE VIEW fv_model FOLDER = '/05 - final views'
    PRIMARY KEY ( 'model_id' ) AS SELECT model_id AS model_id, brand_code AS brand_code, max(typcom) AS model FROM dv_immatriculations_incremental GROUP BY model_id, brand_code;

ALTER VIEW fv_model
 LAYOUT (dv_immatriculations_incremental = [20, 20, 255, 606]);

